//
//  AppIcons.swift
//  NurseryConnect-TabOS
//
//  Centralised SF Symbol names so iconography stays consistent and a
//  single rename re-skins every screen (carried forward from Assignment 1).
//

enum AppIcons {
    // Sidebar / navigation
    static let children = "person.2.fill"
    static let insights = "chart.xyaxis.line"
    static let incidents = "exclamationmark.triangle.fill"

    // Diary entry types
    static let activity = "figure.play"
    static let meal = "fork.knife"
    static let nap = "moon.zzz.fill"
    static let nappy = "drop.fill"
    static let wellbeing = "heart.fill"

    // Affordances
    static let reportIncident = "exclamationmark.bubble.fill"
    static let add = "plus"
    static let signature = "signature"
    static let pencil = "pencil.and.scribble"
    static let allergy = "allergens"
    static let dietary = "leaf.fill"
    static let noPhoto = "camera.fill"
    static let success = "checkmark.circle.fill"
    static let empty = "tray.fill"
}
